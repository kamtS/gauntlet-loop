interface:
  display_name: "Gauntlet Loop"
  short_description: "Route a bounded build-and-critique loop"
  default_prompt: "Use $gauntlet-loop to route this task across Claude Code, Codex, and TFCode with an explicit bounded worker-and-critic policy."
